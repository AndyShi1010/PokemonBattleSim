import java.awt.Color;
import java.io.IOException;

public class GameRunner {
	public static void main (String[] args) throws IOException{
		System.out.println("Game Start");
		DialogWindow nameInputWindow = new DialogWindow();
		nameInputWindow.repaint();//Refreshes window frame on start.
		//while (true); //Prevents program from ending when window is drawn*/
	}	
	
}
